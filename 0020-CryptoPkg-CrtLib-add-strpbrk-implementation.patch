From 5018f8191fe628eb474e8c5fa4e365ddbaca233d Mon Sep 17 00:00:00 2001
From: Gerd Hoffmann <kraxel@redhat.com>
Date: Thu, 24 Apr 2025 12:06:05 +0200
Subject: [PATCH 20/21] CryptoPkg/CrtLib: add strpbrk implementation

Needed by openssl-3.5.1.

Signed-off-by: Gerd Hoffmann <kraxel@redhat.com>
---
 CryptoPkg/Library/Include/CrtLibSupport.h     |  6 ++++++
 .../Library/BaseCryptLib/SysCall/CrtWrapper.c | 19 +++++++++++++++++++
 2 files changed, 25 insertions(+)

diff --git a/CryptoPkg/Library/Include/CrtLibSupport.h b/CryptoPkg/Library/Include/CrtLibSupport.h
index 8663554960be..21780780f046 100644
--- a/CryptoPkg/Library/Include/CrtLibSupport.h
+++ b/CryptoPkg/Library/Include/CrtLibSupport.h
@@ -467,6 +467,12 @@ strcat (
   const char  *strSource
   );
 
+char *
+strpbrk (
+  const char  *s,
+  const char  *accept
+  );
+
 //
 // Macros that directly map functions to BaseLib, BaseMemoryLib, and DebugLib functions
 //
diff --git a/CryptoPkg/Library/BaseCryptLib/SysCall/CrtWrapper.c b/CryptoPkg/Library/BaseCryptLib/SysCall/CrtWrapper.c
index aa0c967c535d..11d01106d4eb 100644
--- a/CryptoPkg/Library/BaseCryptLib/SysCall/CrtWrapper.c
+++ b/CryptoPkg/Library/BaseCryptLib/SysCall/CrtWrapper.c
@@ -425,6 +425,25 @@ strtoul (
   return 0;
 }
 
+char *
+strpbrk (
+  const char  *s,
+  const char  *accept
+  )
+{
+  int  i;
+
+  for ( ; *s != '\0'; s++) {
+    for (i = 0; accept[i] != '\0'; i++) {
+      if (*s == accept[i]) {
+        return (char *)s;
+      }
+    }
+  }
+
+  return NULL;
+}
+
 /* Convert character to lowercase */
 int
 tolower (
-- 
2.49.0

